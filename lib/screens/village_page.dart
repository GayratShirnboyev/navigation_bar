import 'package:flutter/material.dart';

class VillagePage extends StatelessWidget {
  const VillagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Container(color: Colors.yellow,)),
    );
  }
}
